// Retired — not part of the live Watad site (P3 cleanup, 2026-05-21).
// Removed as a duplicate mobile WhatsApp entry point (MobileStickyBar
// already carries WhatsApp). Reduced to an inert stub — file deletion
// is blocked in this environment.
// Safe to `git rm components/FloatingWhatsApp.tsx` from the repository.
export {};
